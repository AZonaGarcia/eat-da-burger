
<li>
    {{ name }}

    <button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
        {{#if devour}}DEVOUR IT!!{{else}}DEVOUR!!{{/if}}
    </button>

    <button class="remove-burger" data-id="{{id}}">REMOVE BURGER</button>
</li>
